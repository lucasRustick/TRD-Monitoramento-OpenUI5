import { user_groups } from "./registerData/user_groups.controller";
import { users } from "./registerData/users.controller";

export const RegisterData = {
    user_groups: user_groups,
    users: users
}